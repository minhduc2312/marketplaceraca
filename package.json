{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "proxy": "http://localhost:5000/",
  "scripts": {
    "server": "nodemon ./backend/server.js ",
    "client": "npm run start --prefix frontend",
    "start": "node server.js --prefix backend",
    "install-backend": "npm install --prefix backend --only=dev ",
    "heroku-postbuild": "npm install --prefix frontend --only=dev && npm run install-backend && npm install && npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^2.0.15"
  },
  "dependencies": {
    "concurrently": "^7.1.0"
  }
}
