{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "proxy": "http://localhost:5000/",
  "scripts": {
    "server": "nodemon ./backend/server.js ",
    "client": "npm run start --prefix frontend",
    "start": "node server.js --prefix backend",
    "heroku-postbuild": "cd frontend && npm install --only=dev && cd backend && npm install --only=dev && npm install && npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^2.0.15"
  },
  "dependencies": {
    "concurrently": "^7.1.0"
  }
}
